# CAD Assembly Pipeline dependencies
#
# Core CAD kernel (cadquery bundles OCP / OpenCascade bindings)
cadquery>=2.4,<3
numpy>=1.24

# Rendering
pyvista>=0.43

# Mesh format support (.stl, .obj, .ply, .3mf)
trimesh>=4.0
scipy>=1.10
rtree>=1.0
manifold3d>=3.0
networkx>=3.0
lxml>=4.9

# Testing
pytest>=7.0

# Web UI
flask>=3.0
livereload>=2.6
# GPU acceleration (optional — install for CUDA-accelerated PCA,
# tessellation, physics simulation, and parallel cutting)
# cupy-cuda12x>=13.0  # Uncomment for CUDA 12.x
# cupy-cuda11x>=13.0  # Uncomment for CUDA 11.x
